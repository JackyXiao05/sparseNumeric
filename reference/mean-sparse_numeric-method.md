# Mean of a sparse_numeric vector

Computes arithmetic mean of `sparse_numeric` vector, including implicit
zeros.

Computes arithmetic mean of `sparse_numeric` vector, including implicit
zeros.

## Usage

``` r
# S4 method for class 'sparse_numeric'
mean(x, ...)

# S4 method for class 'sparse_numeric'
mean(x, ...)
```

## Arguments

- x:

  A `sparse_numeric` vector.

- ...:

  Additional arguments (ignored).

## Value

A numeric scalar giving mean.

A numeric scalar giving mean.
