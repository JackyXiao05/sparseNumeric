# Arithmetic operations for sparse_numeric vectors

Implements elementwise arithmetic for two `sparse_numeric` vectors of
the same length.

Implements elementwise arithmetic for two `sparse_numeric` vectors of
the same length.

## Usage

``` r
# S4 method for class 'sparse_numeric,sparse_numeric'
e1 + e2

# S4 method for class 'sparse_numeric,sparse_numeric'
e1 - e2

# S4 method for class 'sparse_numeric,sparse_numeric'
e1 * e2

# S4 method for class 'sparse_numeric,sparse_numeric'
e1 + e2

# S4 method for class 'sparse_numeric,sparse_numeric'
e1 - e2

# S4 method for class 'sparse_numeric,sparse_numeric'
e1 * e2
```

## Arguments

- e1:

  A `sparse_numeric` vector.

- e2:

  A `sparse_numeric` vector.

## Value

A `sparse_numeric` vector.

A `sparse_numeric` vector.
