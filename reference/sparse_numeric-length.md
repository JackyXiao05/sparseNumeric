# Length of a sparse_numeric vector

Returns the full logical length of the underlying dense vector
represented by a `sparse_numeric` object.

Returns the full logical length of the underlying dense vector
represented by a `sparse_numeric` object.

## Usage

``` r
# S4 method for class 'sparse_numeric'
length(x)

# S4 method for class 'sparse_numeric'
length(x)
```

## Arguments

- x:

  A `sparse_numeric` vector.

## Value

An integer scalar giving the length.

An integer scalar giving the length.
