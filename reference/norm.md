# Euclidean norm of a sparse_numeric vector

Computes Euclidean (L2) norm \\sqrt(sum(x^2))\\ of a `sparse_numeric`
vector.

Computes Euclidean (L2) norm \\sqrt(sum(x^2))\\ of a `sparse_numeric`
vector.

## Usage

``` r
norm(x, type, ...)

# S4 method for class 'sparse_numeric,ANY'
norm(x, type, ...)

norm(x, type, ...)

# S4 method for class 'sparse_numeric,ANY'
norm(x, type, ...)
```

## Arguments

- x:

  A `sparse_numeric` vector.

- type:

  Ignored; included for compatibility with
  [`norm`](https://rdrr.io/r/base/norm.html).

- ...:

  Additional arguments (ignored).

## Value

Numeric scalar giving Euclidean norm.

Numeric scalar giving Euclidean norm.
